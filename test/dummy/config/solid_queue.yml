default: &default
  queues:
    default:
      worker_count: 3
      polling_interval: 5
    background:
      worker_count: 3
  scheduler:
    polling_interval: 300
    batch_size: 500

development:
  <<: *default

test:
  <<: *default
