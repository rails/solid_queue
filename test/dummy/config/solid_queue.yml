default: &default
  workers:
    background:
      pool_size: 3
    default:
      pool_size: 5
  scheduler:
    polling_interval: 1
    batch_size: 500

development:
  <<: *default

test:
  <<: *default
