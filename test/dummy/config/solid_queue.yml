default: &default
  queues:
    background:
      worker_count: 3
    default:
      worker_count: 5
  scheduler:
    polling_interval: 300
    batch_size: 500

development:
  <<: *default

test:
  <<: *default
